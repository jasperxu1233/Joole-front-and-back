spring:

  main:
    allow-circular-references: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Xuhaojie1233
    url: jdbc:mysql://127.0.0.1:3306/joole?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect


server:
  servlet:
    context-path: /joole
  port: 8080
#    context-path:postman测试的8080后面的uri

